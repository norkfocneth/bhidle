namespace TERRAGRAV.Core
{
    /// <summary>
    /// Lifecycle states for the match and game flow.
    /// </summary>
    public enum GameState
    {
        Boot,
        Lobby,
        Countdown,
        Playing,
        GameOver
    }
}
